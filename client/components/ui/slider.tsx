// Slider removed: this component is intentionally stubbed because a fixed header banner replaced the original slider usage.
// Keeping a stub avoids import errors until all references are cleaned up.
import * as React from "react";

export const Slider = () => null;
export default Slider;
